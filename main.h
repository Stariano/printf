#ifndef MAINH
#define MAINH

#include <stdlib.h>
#include <stdarg.h>
#include <string.h>

int _printf(const char *format, ...);
char *_itoa(int i, char *strout, int base);
char *_strrev (char *str);

#endif /* MAINH */
